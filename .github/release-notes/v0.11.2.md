## Fixed

- Fix blank terminal views after opening SSH or local connections. The terminal page now includes the serializer script required for startup.
- Add a regression check that verifies every terminal script and stylesheet is included in the page, in the correct order.

## Validation

- Verified that the local PowerShell banner, prompt, typed command, and command output are visible.
- All 201 terminal tests passed locally.

[Full changelog](https://github.com/bpbp-boop/resesh/compare/v0.11.1...v0.11.2)
