<ContentDialog
    x:Class="Sessions.App.Dialogs.ImportBackupDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Sessions.App.Dialogs"
    Title="Import backup"
    PrimaryButtonText="Import"
    CloseButtonText="Cancel"
    DefaultButton="Primary">

    <StackPanel Spacing="12" MinWidth="560">
        <TextBlock x:Name="SummaryText" TextWrapping="Wrap" />
        <InfoBar
            IsOpen="True"
            Severity="Informational"
            Message="App settings are restored. Known hosts and highlight rules are merged. An existing different host key is never replaced." />
        <TextBlock
            x:Name="ConflictHeader"
            Text="Resolve conflicts"
            FontWeight="SemiBold"
            Visibility="Collapsed" />
        <ListView
            x:Name="ConflictList"
            MaxHeight="310"
            SelectionMode="None"
            ItemsSource="{x:Bind Conflicts}"
            Visibility="Collapsed">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:BackupConflictViewModel">
                    <Grid Padding="0,6" ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="150" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Spacing="2">
                            <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" />
                            <TextBlock Text="{x:Bind Detail}" Opacity="0.65" FontSize="12" TextWrapping="Wrap" />
                        </StackPanel>
                        <ComboBox
                            Grid.Column="1"
                            ItemsSource="{x:Bind Choices}"
                            DisplayMemberPath="Label"
                            SelectedItem="{x:Bind SelectedChoice, Mode=TwoWay}"
                            HorizontalAlignment="Stretch" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</ContentDialog>
