<ContentDialog
    x:Class="Resesh.App.Dialogs.ImportPreviewDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Resesh.App.Dialogs"
    Title="Import from SecureCRT"
    PrimaryButtonText="Import"
    CloseButtonText="Cancel"
    DefaultButton="Primary">

    <StackPanel Spacing="12" MinWidth="520">
        <TextBlock x:Name="SummaryText" TextWrapping="Wrap" />

        <ListView
            x:Name="CandidateList"
            MaxHeight="320"
            SelectionMode="None"
            ItemsSource="{x:Bind Candidates}">
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="local:ImportCandidateVm">
                    <CheckBox IsChecked="{x:Bind IsSelected, Mode=TwoWay}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <TextBlock Text="{x:Bind Name}" FontWeight="SemiBold" />
                            <TextBlock Text="{x:Bind Detail}" Opacity="0.6" VerticalAlignment="Center" FontSize="12" />
                        </StackPanel>
                    </CheckBox>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Expander
            x:Name="SkippedExpander"
            Header="Skipped (not SSH)"
            HorizontalAlignment="Stretch"
            Visibility="Collapsed">
            <ItemsControl x:Name="SkippedList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <TextBlock Text="{x:Bind}" Opacity="0.7" FontSize="12" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Expander>

        <TextBlock
            Text="Passwords are not imported — SecureCRT stores them encrypted. Imported sessions will prompt on first connect and can save to Windows Credential Manager."
            TextWrapping="Wrap"
            Opacity="0.7"
            FontSize="12" />
    </StackPanel>
</ContentDialog>
